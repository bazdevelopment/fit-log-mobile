import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    height: 45,
  },
  activeBox: {
    position: "absolute",
    borderRadius: 10,
    elevation: 3,
    height: "80%",
    top: "10%",
  },
});
