/**
Description:
The isOdd function is designed to determine whether a given number is odd or not. It returns true if the provided number is odd, and false if it is even.
*/
export function isOdd(number: number): boolean {
  return number % 2 !== 0;
}
